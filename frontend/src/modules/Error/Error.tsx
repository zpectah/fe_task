import { ViewLayout } from '../../components';

const Error = () => {
  return (
    <ViewLayout maxWidth="md" heading={{ title: 'Error 404' }}>
      ...Error view...
    </ViewLayout>
  );
};

export default Error;
